1. `git pull` 실행
   - 충돌이나 문제 발생하면 바로 중단하고 알려줘
   - 정상적으로 받아졌으면 다음 단계로

2. `git status`로 변경사항 확인

3. 변경사항 분석해서 커밋 메시지 작성 (한글로)
   - conventional commit 형식: feat, fix, refactor, docs, style, chore 등
   - 예: "feat: 간트차트 드래그앤드롭 기능 추가"

4. `git add .` → `git commit` → `git push` 순서로 실행